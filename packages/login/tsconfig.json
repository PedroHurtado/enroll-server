{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./"  // Cambia esto a la raíz del paquete "server"
  },
  "include": [
    "src/**/*"
  ],
  "exclude": [
    "src/node_modules"
  ]
}

