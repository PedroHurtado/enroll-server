export enum Identity {
    Student = "student",
    Mother = "mother",
    Father = "father",
    Guardian = "guardian"
}

export enum Situation {
    Single = "single",
    Married = "married",
    Divorced = "divorced",
    Widowed = "widowed",
}

export enum CustodyPA {
    Mother = "mother",
    Father = "father",
    Both = "both"
}